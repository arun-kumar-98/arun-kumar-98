server:
  port: 9090
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=health
    password: root
    username: postgres
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQL91Dialect
    show-sql: true
    hibernate:
      ddl-auto: update
    